# Declares which locale the unqualified values/ directory is written in.
# Required by androidResources.generateLocaleConfig so the generated <locale-config>
# can list English alongside the values-<locale>/ directories.
unqualifiedResLocale=en-US
